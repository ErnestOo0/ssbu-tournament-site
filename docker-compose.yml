services:
  serveur:
    image: serveur
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 8080:8080

  client:
    image: client
    build: 
      context : ./frontend
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000




